Review the deployment script for user-friendliness and out-of-the-box experience.

Make sure that:
- deployment is easy and requires minimal configuration,
- the agblogger server will work when deploying from a local machine to a remote server, 
- it is possible to deploy to a remote server without any additional configuration,
- it is possible to upgrade an existing installation without data loss,
- the script handles upgrading an existing installation gracefully.
