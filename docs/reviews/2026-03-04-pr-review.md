# PR Review — 2026-03-04

Comprehensive review of `origin/main..HEAD` (107 files, 15 commits).

## Critical (C)

| ID | File | Issue | Fix |
|----|------|-------|-----|
| C1 | `useCodeBlockEnhance.ts:40` | `codeEl.textContent` may be `null` → `writeText(null)` throws | `codeEl.textContent ?? ''` |
| C2 | `admin.py:198-216` | Password change revokes sessions but response doesn't inform frontend; frontend doesn't logout | Return `sessions_revoked: true`; frontend calls `logout()` |
| C3 | `posts.py`, `admin.py` | `try_commit` can return `None` on git failure; callers ignore it silently | Add `X-Git-Warning` response header when `try_commit` returns `None` |
| C4 | `crosspost.py:56-62` | `_safe_status` falls back to `FAILED` for unknown values, masking data corruption | Add `UNKNOWN` status; log at ERROR level |

## Important (I)

| ID | File | Issue | Fix |
|----|------|-------|-----|
| I1 | `auth.py:127-133` | `_reject_browser_originated_token_login` checks Referer but has no test for it | Add regression test |
| I2 | `ShareBar.tsx:34` | `void handleNativeShare()` silently swallows non-AbortError rejections as unhandled | `.catch(() => {})` |
| I3 | `themeStore.ts:41,47` | `localStorage` calls can throw (quota exceeded, private browsing) | Wrap in try-catch with fallback |
| I4 | `posts.py:582` | `except RuntimeError` doesn't catch `OSError` from pandoc | Widen to `except (RuntimeError, OSError)` |
| I5 | `useCodeBlockEnhance.ts` | No error boundary around DOM manipulation in MutationObserver | Wrap `enhanceCodeBlocks()` calls in try-catch |
| I6 | `admin.py:198` | Password change endpoint has no rate limiting | Add `InMemoryRateLimiter` check |
| I7 | `useShareHandlers.ts` | Hook is completely untested | Add comprehensive test file |
| I8 | `useCodeBlockEnhance.ts:47` | Clipboard rejection handler ignores the error argument | Add `(err: unknown)` param, `console.warn` |

## Suggestions (S)

| ID | File | Issue | Fix |
|----|------|-------|-----|
| S1 | `auth_service.py:163` | `revoke_user_credentials` docstring doesn't mention caller must commit | Add note to docstring |
| S2 | `admin.py:205-206` | Manual password-match check duplicates what a model_validator could do | Add `@model_validator(mode="after")` to `PasswordChange` |
| S4 | `ShareBar.tsx`, `useShareHandlers.ts` | Duplicate `ShareProps`-style interfaces | Extract to `shareTypes.ts` |
| S5 | `SocialAccountsPanel.tsx:32-39` | Local `formatDate` duplicates `@/utils/date` | Import from shared utility |
| S6 | `client.ts:215-218` | `TokenResponse` interface appears unused by frontend | Remove dead code |
| S7 | `content.py:57` | `_check_draft_access` docstring doesn't mention symlink resolution | Add note |
| S9 | `sync.py:418` | Missing logger.warning when `commit_exists` returns False | Add warning |
| S10 | `date.ts:23,40` | Fallback `dateStr.split(' ')[0]` truncates multi-word invalid input | Use `dateStr \|\| ''` |
| S11 | Post rename symlink | No test verifying content accessible via old symlink path | Add test |
| S12 | `AdminPage.test.tsx` | Repeated tab-switching boilerplate | Extract `switchToTab` helper |
