# Snyk Code Analysis Review — 2026-03-12

Snyk reported 23 open issues (13 HIGH, 10 MEDIUM). All 23 are **false positives** — the codebase already has proper input validation at the appropriate boundaries that Snyk's static taint analysis cannot trace through.

## Command Injection — CLI tools (3 MEDIUM, all false positives)

| Finding | File | Line |
|---------|------|------|
| dc97af84 | cli/local_caddy.py | 59 |
| 672915cd | cli/local_caddy.py | 64 |
| ee90f689 | cli/zap_scan.py | 104 |

**Why false positive**: These are local CLI tools, not web-facing endpoints. They use `asyncio.create_subprocess_exec` (not `shell=True`), and the arguments come from `argparse` with validated types (`Path`, `validate_port`). CLI tools inherently trust operator-provided command-line arguments. No injection vector exists.

## Open Redirect — crosspost.py (7 MEDIUM, all false positives)

| Finding | Line |
|---------|------|
| ed5b406d | 462 |
| a39824b4 | 652 |
| 0cff85ef | 722 |
| e79eeb82 | 794 |
| 46e11f65 | 866 |
| d676e3d8 | 951 |
| 776955b7 | 965 |

**Why false positive**: All `RedirectResponse` calls redirect to `f"{base_url}/admin"` or `f"{base_url}/admin?{params}"` where `base_url` comes from `settings.bluesky_client_url` — a **server-side configuration value**, not from the HTTP request. Snyk misidentifies the data source.

## Path Traversal — admin.py (5 HIGH, all false positives)

| Finding | Line | Endpoint |
|---------|------|----------|
| bc42f9f9 | 87 | PUT /api/admin/site |
| 853512bc | 126 | POST /api/admin/pages |
| 60a2aafb | 155 | PUT /api/admin/pages/order |
| b6bc7f87 | 179 | PUT /api/admin/pages/{page_id} |
| 630cd312 | 204 | DELETE /api/admin/pages/{page_id} |

**Why false positive**:

- **Line 87** (`update_site_settings`): Takes `title`/`description`/`timezone` — no path manipulation at all.
- **Line 126** (`create_page`): `body.id` is validated by Pydantic with `pattern=r"^[a-z0-9][a-z0-9_-]*$"` (`backend/schemas/admin.py:54`). Slashes and `..` are impossible.
- **Line 155** (`update_page_order`): Rewrites page config only, no file path creation from user input.
- **Lines 179, 204** (`update_page`, `delete_page`): `page_id` checked against `_PAGE_ID_PATTERN = re.compile(r"^[a-z0-9][a-z0-9_-]*$")` at lines 175 and 200. Path traversal impossible.

All admin endpoints also require admin authentication via `require_admin`.

## Path Traversal — labels.py (2 HIGH, all false positives)

| Finding | Line | Endpoint |
|---------|------|----------|
| 37be4481 | 58 | POST /api/labels |
| c5d8aaf0 | 75 | PUT /api/labels/{label_id} |

**Why false positive**: Label IDs (`LabelIdRef`) are constrained to `^[a-z0-9][a-z0-9-]*$` by the Pydantic schema (`backend/schemas/label.py:12`). They're used as TOML keys and DB identifiers, not filesystem paths. Both endpoints require admin authentication.

## Path Traversal — content.py (1 HIGH, false positive)

| Finding | Line | Endpoint |
|---------|------|----------|
| 4419fc96 | 157 | GET /api/content/{file_path:path} |

**Why false positive**: The `_validate_path()` function (`content.py:37-67`) applies three layers of defense:

1. Rejects `..` path components
2. Requires `posts/` or `assets/` prefix
3. Resolves the path and verifies `is_relative_to(content_dir.resolve())`

## Path Traversal — posts.py (2 HIGH, all false positives)

| Finding | Line |
|---------|------|
| 694b58ed | 884 |
| 27198d3b | 919 |

**Why false positive**: Both `shutil.move` calls operate on paths derived from:

- `old_dir`: the existing post directory from the DB record, within content_dir
- `new_dir`: constructed as `posts_parent / f"{date_prefix}-{new_slug}"` where `posts_parent` is the parent of `old_dir` and the slug is generated by slugifying the title

Both are constrained within the content directory. No user-controlled path traversal vector.

## Path Traversal — sync.py (1 HIGH, false positive)

| Finding | Line | Endpoint |
|---------|------|----------|
| dbc755aa | 186 | GET /api/sync/download/{file_path:path} |

**Why false positive**: `_resolve_safe_path()` (`sync.py:50-58`) validates:

1. Only sync-managed paths via `is_sync_managed_path()`
2. Resolves and checks `is_relative_to(content_dir.resolve())`

Also requires admin authentication via `require_admin`.

## XSS — main.py (2 HIGH, all false positives)

| Finding | Line |
|---------|------|
| d2266db4 | 740 |
| 3f0964ba | 743 |

**Why false positive**: Both lines return `HTMLResponse(base_html)` where `base_html` is the static `index.html` file cached from disk — no user input is injected. The enriched response at line 762 (not flagged by Snyk) is also safe because `inject_og_tags()` HTML-escapes all values with `html.escape()` (`backend/services/opengraph_service.py:50-52`).

## Summary

| Category | Count | Severity | Verdict |
|----------|-------|----------|---------|
| CLI Command Injection | 3 | MEDIUM | False positive — local CLI tools, not web-facing |
| Open Redirect | 7 | MEDIUM | False positive — redirect URLs from server config, not HTTP input |
| Path Traversal (admin) | 5 | HIGH | False positive — Pydantic regex + endpoint regex validation |
| Path Traversal (labels) | 2 | HIGH | False positive — Pydantic regex on label IDs |
| Path Traversal (content) | 1 | HIGH | False positive — `_validate_path()` with 3-layer defense |
| Path Traversal (posts) | 2 | HIGH | False positive — paths derived from DB records + slug generation |
| Path Traversal (sync) | 1 | HIGH | False positive — `_resolve_safe_path()` + admin auth |
| XSS | 2 | HIGH | False positive — static HTML from disk, not user input |
| **Total** | **23** | | **All false positives** |

Snyk's static taint analysis cannot trace through Pydantic schema validation, regex guards, path resolution checks, or HTML escaping — it flags data flows from HTTP input to sensitive sinks without recognizing the sanitization in between.
